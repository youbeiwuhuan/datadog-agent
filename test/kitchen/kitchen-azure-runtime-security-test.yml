suites:

# Deploys and run the functional tests
- name: dd-agent-runtime-security-test
  run_list:
    - "recipe[dd-agent-rhel-workaround]"
    - "recipe[dd-agent-sles-workaround]"
    - "recipe[dd-agent-runtime-security-check]"
  attributes:
    apt:
      unattended_upgrades:
        enable: false
